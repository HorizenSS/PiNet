@model IEnumerable<PiDev.Domain.Entities.reponse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.question.quesText)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.isValide)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.question.quesText)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.isValide)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idRep }) |
            @Html.ActionLink("Details", "Details", new { id=item.idRep }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idRep })
        </td>
    </tr>
}

</table>
